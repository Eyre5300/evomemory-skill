{
  "last2_3pow2024": "For the last two digits of a^n, compute a^n mod 100. Find the multiplicative order of a modulo 100 (the smallest d with a^d ≡ 1 mod 100), reduce the exponent n modulo d, then compute the small remaining power. For 3 modulo 100 the order is 20, so reduce the exponent modulo 20. Pitfall: reduce the exponent by the ORDER d, not by 100; verify d by checking a^d ≡ 1 (mod 100) before using it.",
  "no_zero_pairs": "Count a in 1..999 with both a and 1000-a free of the digit 0, by analyzing the subtraction 1000 - a digit by digit with borrows, handling 1-, 2-, and 3-digit a separately (leading zeros are NOT written, so they don't count as a 0 digit). A reliable route: for each number of digits of a, count choices for each digit position so that the corresponding digit of b = 1000 - a is also nonzero, tracking the borrow. Pitfall: the borrow chain links the digit constraints; verify your method on a smaller analogue like a+b=100 before trusting it on 1000."
}
